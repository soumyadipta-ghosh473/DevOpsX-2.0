apiVersion: v1
kind: Service
metadata:
  name: devopsx-service
spec:
  type: NodePort
  selector:
    app: devopsx-app
  ports:
  - protocol: TCP
    port: 5000
    targetPort: 5000
    nodePort: 30080
